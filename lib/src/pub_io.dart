library;

import '../io.dart';

export '../io.dart';

// obsolete
/// Obsolete: use [PubPackage].
class IoPubPackage extends PubPackage {
  /// Constructor for [IoPubPackage].
  IoPubPackage(super.path);
}
